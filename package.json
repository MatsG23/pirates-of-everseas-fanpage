{
  "name": "pirates-of-everseas-fanpage",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://matsg23.github.io/pirates-of-everseas-fanpage",
  "dependencies": {
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-scripts": "4.0.2"
  },
  "scripts": {
    "start": "SET BROWSER=none && react-scripts start",
    "predeploy": "SET INLINE_RUNTIME_CHUNK=false && react-scripts build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
